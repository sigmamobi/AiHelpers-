# AI Assistant App - Environment Variables
# Copy this file to .env and fill in your values

# Supabase Configuration
# Get these from your Supabase project dashboard (Settings → API)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key
# Service role key for Edge Functions (IMPORTANT: never expose to client code)
# Find this in Settings → API → service_role key
SERVICE_ROLE_KEY=your-service-role-key

# OpenAI Configuration
# Get this from https://platform.openai.com/account/api-keys
OPENAI_API_KEY=your-openai-api-key

# Expo/App Configuration
# Environment: development, staging, production
EXPO_PUBLIC_APP_ENV=development

# Web App URL (for redirects after authentication)
# For local development with Expo Web:
APP_URL=http://localhost:8082
# For production, use your deployed URL:
# APP_URL=https://your-app-domain.com

# Optional: Analytics and Monitoring
# SENTRY_DSN=your-sentry-dsn
# EXPO_PUBLIC_ANALYTICS_ID=your-analytics-id

# Optional: Storage Configuration (if using S3 or other storage)
# S3_BUCKET=your-s3-bucket
# S3_REGION=your-s3-region
# S3_ACCESS_KEY=your-s3-access-key
# S3_SECRET_KEY=your-s3-secret-key

# CORS Configuration
# Make sure to configure these URLs in Supabase Dashboard:
# Authentication → URL Configuration → Site URL and Redirect URLs
# For local: http://localhost:8082
# For production: https://your-app-domain.com
